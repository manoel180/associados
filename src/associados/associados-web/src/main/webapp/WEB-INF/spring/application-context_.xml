<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

	<!-- enable use-expressions -->
	<http auto-config="true" use-expressions="true">

		<intercept-url pattern="/login.jsf" access="permitAll" />
		<intercept-url pattern="/home.jsf" access="isAuthenticated()"/>
		<intercept-url pattern="/resources/**" access="isAnonymous()" />

		<!-- access denied page -->
		<access-denied-handler error-page="/403" />

		<form-login login-page="/login.jsf" default-target-url="/home.jsf"
			authentication-failure-url="/login.jsf?error" username-parameter="username"
			password-parameter="password" />
		<logout logout-success-url="/login.jsf?logout" />
		<!-- enable csrf protection -->
		<!-- <csrf/> -->
	</http>

	<!-- Select users and user_roles from database -->
	<!-- Authentication manager. -->
	<authentication-manager alias="authenticationManager">
	</authentication-manager>

</beans:beans>